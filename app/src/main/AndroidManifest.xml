<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khata_book_application">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Khata_Book_Application">
        <activity
            android:name=".View.Money_Show_Activity"
            android:exported="false" />
        <activity
            android:name=".View.New_Contect_Activity"
            android:exported="false" />
        <activity
            android:name=".View.Add_Custmer_Data"
            android:exported="false" />
        <activity
            android:name=".View.Flash_Screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.MainActivity"
            android:exported="true" />
    </application>

</manifest>